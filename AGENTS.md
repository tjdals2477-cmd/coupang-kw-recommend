# Repository rules

- 이 저장소는 `coupang-kw`와 별도 배포한다. 코어 저장소의 모듈을 import하거나 런타임 의존하지 않는다.
- 코어와의 유일한 인터페이스는 산출 엑셀 및 동반 `contract.json`이다.
- API 자격증명은 `.env`에서만 읽으며 코드, 설정, 테스트 fixture에 기록하지 않는다.
- 운영 상수, 시트명, 컬럼 별칭, 필터 및 점수 가중치는 패키지의 `templates/recommend.yaml`에서 로드한다.
- 입력 오류와 API 청크 실패는 전체 실행을 중단시키지 않는다. 경고 또는 실패 레코드로 남기고 처리 가능한 범위를 계속한다.
- 출력 시트명과 컬럼명은 외부 계약이므로 명시적인 스키마 변경 없이 바꾸지 않는다.
- `kwnorm.py`는 코어와 수동 동기화하는 벤더 파일이며 표준 라이브러리 외 의존성을 추가하지 않는다.
